[pytest]
testpaths = tests
markers =
    slow: exercises heavier integration-style paths (a real model forward/train/imagine pass, or a real HTTP round trip). Skip during fast local iteration with: pytest -m "not slow"
asyncio_mode = auto