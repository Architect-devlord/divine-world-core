# Python version: 3.12 recommended for full compatibility
# Python 3.13 requires cassandra-driver>=4.1.0, torch 2.4.0+
# Use python3.12 for most stable experience
# Core ML/DL
numpy>=2.3.2           # Required for official Python 3.14 wheels
torch>=2.5.0           # Minimum for stable 3.13/3.14 support
torchvision>=0.20.0

#download pytorch using pip install torch torchvision torchaudio  --index-url https://download.pytorch.org/whl/cpu if no gpu is present and you want to save space


# RL Libraries
stable-baselines3>=2.8.0 # Official support for Python 3.13+
torchrl>=0.6.0
gymnasium>=1.0.0

# Continual Learning
avalanche-lib>=0.6.0   # Ensure 3.13 compatibility

# Backend
fastapi>=0.115.0       # Better Pydantic 2.10 integration
uvicorn>=0.31.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
pydantic>=2.10.0
aiofiles>=24.1.0
# opencv-python-headless>=4.11.0
# Vision/Audio 
opencv-python>=4.11.0  # Updated for latest Python builds
pillow>=11.0.0         # Critical for 3.13/3.14 support

#download opencv-python-headless and opencv-python using 
#pip install opencv-python-headless --only-binary=:all:
 
# Utilities
scipy>=1.15.0          # Required for NumPy 2.x compatibility
psutil>=5.9.0
tqdm>=4.65.0
playwright>=1.48.0
# then use playwright install chromium 

# Optional: GUI
# PyQt5>=5.15.0  # For utils/gui.py

websockets>=11.0  # Used in unified_chat_system
pyinstaller>=6.11.0    # Improved support for Python 3.13+ internals
flask>=2.0.0  # For web interface
flask-socketio>=5.0.0  # For real-time communication in web interface
sounddevice>=0.4.6  # For microphone input
# Object storage and Scylla/Cassandra driver
boto3>=1.35.0
scylla-driver>=3.26.0  # Supports Python 3.10 through 3.14
msgpack>=1.0.5
aiohttp>=3.10.0
beautifulsoup4>=4.12.2
lxml>=4.9.3
pyaudio>=0.2.13
SpeechRecognition>=3.11.0
librosa>=0.10.0
soundfile>=0.12.1
lz4>=4.3.2
python-snappy>=0.6.1

#--extra-index-url https://pypi.nvidia.com
#isaacsim[BUNDLE]==5.1.0

# isaac sim stuff for later
# only core packages 
# pip install isaacsim --index-url https://pypi.nvidia.com
# every thing of isaacsim
# pip install isaacsim[all] --index-url https://pypi.nvidia.com
# caching extras with everything 
# pip install isaacsim[all,extscache] --index-url https://pypi.nvidia.com
# after either 3 of them 
# Locate your installation directory
# pip show isaacsim
# Run the target compatibility checker app
# isaacsim isaacsim.exp.compatibility_check

# python 3.11 for isaac sim 5 and python 3.12 for isaac sim 6

# For scylla in linux do these before running the main.py
# Check if it's installed
# scylla --version
# If not installed then use
# yay -S scylla-bin

#Run Scylla's built-in benchmark tool. It will test your disk performance and generate the necessary configuration file (/etc/scylla.d/io.conf).

# sudo /usr/lib/scylla/scylla_io_setup
# Note: This might take a few minutes as it stresses your disk.

# If installed via systemd:
# sudo systemctl start scylla-server
# sudo systemctl status scylla-server

# Or via Docker (quickest way to get it running):
# docker run --name scylla -p 9042:9042 -d scylladb/scylla

# Confirm it's listening once started:
# ss -tlnp | grep 9042
